"""
Presentation layer (API).

The only layer permitted to know about HTTP. Translates HTTP requests
into Application layer calls and Application layer results back into
HTTP responses. Contains no business logic.

See: 06-Repository-Structure.md §4.
"""
